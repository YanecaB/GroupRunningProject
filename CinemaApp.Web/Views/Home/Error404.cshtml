@{
    Layout = null; // No shared layout
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background: linear-gradient(to bottom, #4caf50, #a8e063);
            color: #fff;
            font-family: 'Comic Sans MS', cursive, sans-serif;
            text-align: center;
            margin: 0;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .content {
            background: rgba(0, 0, 0, 0.6);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.3);
            max-width: 600px;
        }

        .error-icon {
            font-size: 5rem;
        }

        .display-1 {
            font-family: 'Shrek', sans-serif;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }

        .lead {
            font-size: 1.5rem;
        }

        .btn-hover-effect {
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

            .btn-hover-effect:hover {
                transform: scale(1.1);
                background-color: #85bb65;
            }

        .shrek-quote {
            font-style: italic;
            font-size: 1.2rem;
            margin-top: 20px;
            color: #ffeb3b;
        }

        .ogre-icon {
            font-size: 6rem;
        }
    </style>
</head>
<body>
    <div class="content">
        <div class="ogre-icon">🧅</div>
        <h1 class="display-1 fw-bold text-warning">404</h1>
        <p class="lead">Oops! This page is as lost as Donkey in the swamp!</p>
        <p class="mb-4">"What are you doing in my swamp?!" - Shrek</p>
        <a href="/" class="btn btn-primary btn-lg btn-hover-effect">
            <i class="bi bi-arrow-left-circle"></i> Back to Home
        </a>
        <div class="shrek-quote">
            "Onions have layers, and so does this website. Try peeling back to find the right page!"
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
